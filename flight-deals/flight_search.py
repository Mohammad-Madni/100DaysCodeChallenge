import requests
from datetime import datetime
import os
from dotenv import load_dotenv

load_dotenv()


class FlightSearch:
    def get_destination_code(self, city_name):
        code = "testing"
        return code
